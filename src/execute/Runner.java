package execute;

/*
import random.CustomRandom;
import random.RandomData;
*/

public class Runner {

	public static void main(String[] args) {
		// Creates ArrayList of character that are accepted
		// Creates reverse ArrayList
		/*
		 * Randomizer.startup(); Encry_Decry.Setup(); Setup.dialog();
		 */
		// Experimental
		/*
		 * RandomData custom = new CustomRandom();
		 * System.out.println(Setup.settings.key);
		 * //System.out.println(custom.randomNumber()); custom.generateKey(5, 5, 5);
		 * System.out.println(Setup.settings.key);
		 */
	}
}
